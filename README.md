# v-kernelx

This repository contains one canonical state model used across:
- Rust core engine
- TypeScript SDK/runtime
- Python simulation tools

The canonical state format is `v.kernelx/VectorStateV1`.
